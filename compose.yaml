services:
  acme.cinedex.moviesservice.webservice:
    image: acme.cinedex.moviesservice.webservice
    build:
      context: .
      dockerfile: src/Presentation/Acme.Cinedex.MoviesService.WebService/Dockerfile
    ports:
      - "9000:8080"